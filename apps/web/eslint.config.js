// Inherit the shared workspace ESLint config. The Next.js + core-web-vitals
// rule block in @amplience/quadratic-config is already scoped to apps/web/**,
// so no per-app extension is needed here.
export { default } from '@amplience/quadratic-config/eslint'
