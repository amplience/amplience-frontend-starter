// Inherit the shared workspace ESLint config. The Next.js + core-web-vitals
// rule block in @amplience/frontend-starter-config is already scoped to apps/web/**,
// so no per-app extension is needed here.
export { default } from '@amplience/frontend-starter-config/eslint'
