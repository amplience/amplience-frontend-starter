// Workspace root. Package entrypoints live in apps/* and packages/*.
// This file exists so `tsc --noEmit` has an input to typecheck against
// an otherwise empty scaffold (per QL-19 Definition of Done).
export {}
