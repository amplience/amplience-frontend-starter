// Root Vitest config — re-exports the shared preset. Individual packages can
// import this as a base and merge overrides where needed (e.g. jsdom env in
// packages/components).
export { default } from '@amplience/quadratic-config/vitest'
