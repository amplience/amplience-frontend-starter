{
  "name": "@amplience/frontend-starter-content",
  "version": "0.0.0",
  "private": true,
  "description": "ContentClient port + MockContentClient (POC). Per ADR-0008 — the integration seam between the renderer and the Amplience SDK. Real SDK adapter lands in QL-43.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./mock": "./src/mock/index.ts",
    "./sdk": "./src/sdk/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "dc-delivery-sdk-js": "^1.4.0"
  },
  "devDependencies": {
    "@amplience/frontend-starter-config": "workspace:*"
  }
}
