{
  "name": "@amplience/quadratic-content",
  "version": "0.0.0",
  "private": true,
  "description": "ContentClient port + MockContentClient (POC). Per ADR-0008 — the integration seam between the renderer and the Amplience SDK. Real SDK adapter lands in QL-43.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./mock": "./src/mock/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@amplience/quadratic-config": "workspace:*"
  }
}
